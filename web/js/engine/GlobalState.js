/**
 * Contains global state that change through the course of the game
 */
"use strict";

window.GlobalState = {
    /**
     * Price multiplier (how much it costs to buy one unit of resources)
     */
    priceMultiplier: 1
}